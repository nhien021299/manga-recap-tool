{
  "name": "manga-recap-tool",
  "private": true,
  "workspaces": [
    "web-app"
  ],
  "scripts": {
    "dev:be": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000 --app-dir ai-backend",
    "dev:web": "npm --prefix web-app run dev",
    "build:web": "npm --prefix web-app run build",
    "lint:web": "npm --prefix web-app run lint"
  }
}
